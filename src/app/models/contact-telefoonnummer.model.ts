import {Contact} from './contact.model';

export class Telefoon {
  // constructor(private id: number, private telnr: string, private contactId: number) {}
  constructor(private id: number, private telnr: string, private contactId: Contact) {}
}
//
// {}
